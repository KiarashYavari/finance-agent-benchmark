{
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "edgar_search",
        "description": "Search SEC EDGAR for 10-K filings.",
        "parameters": {
          "type": "object",
          "properties": {
            "company": {"type": "string", "description": "Company name or ticker"},
            "period": {"type": "string", "description": "Filing year, e.g., 2023"}
          },
          "required": ["company", "period"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "serp_search",
        "description": "Search the web for information using SERPAPI (rich organic results).",
        "parameters": {
          "type": "object",
          "properties": {"query": {"type": "string", "description": "The query to search for"}},
          "required": ["query"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "google_search",
        "description": "Google web search for financial context.",
        "parameters": {
          "type": "object",
          "properties": {
            "query": {"type": "string", "description": "Search query"}
          },
          "required": ["query"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "parse_html_page",
        "description": "Parse HTML from 10-K filings (bs4).",
        "parameters": {
          "type": "object",
          "properties": {
            "html": {"type": "string", "description": "Raw HTML content"}
          },
          "required": ["html"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "retrieve_information",
        "description": "Retrieve cached/external info.",
        "parameters": {
          "type": "object",
          "properties": {
            "query": {"type": "string", "description": "Info query"}
          },
          "required": ["query"]
        }
      }
    }
  ]
}